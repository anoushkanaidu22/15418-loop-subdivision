# 15418-loop-subdivision

To run the sequential ```loop_subdivision.cpp```, do
```
make clean && make
./loop_subdivision <path to obj file>  <num of iterations of loop subdivision>
```

To run the CUDA implementation ```loop_subdivision_cuda.cu```, do
```
make clean && make
./loop_cuda <path to obj file> [num of iterations of loop subdivision]
```

